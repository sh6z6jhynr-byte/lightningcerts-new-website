# Lightning Certs — Apple-feeling website

A fresh, standalone Lightning Certs marketing site designed around an Apple-inspired product-launch aesthetic.

Open `index.html` to preview. It is plain HTML/CSS/JS, so it can be deployed directly to GitHub Pages, Vercel, Netlify or another static host.

The email form is a front-end demo and needs a real form/email service before production use.
